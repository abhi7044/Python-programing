word = "Python"

# word[2] = 'h'    This line give error because in python the strings are immutable ===> immutable means we can't change randomle any word

print(word[2])

for ch in word:
    print(ch)